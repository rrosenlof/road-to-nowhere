import React from 'react';

const Map = ({ url }) => (
    <div>
        <div>
            <img
                src={url}
                alt='Selected map area'
            />
        </div>
    </div>
)

export default Map